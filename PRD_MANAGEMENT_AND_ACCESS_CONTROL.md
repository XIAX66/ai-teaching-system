# 产品需求文档 (PRD) - 教学资源管理、对话编辑与权限控制

## 1. 功能概述
本阶段开发目标是增强系统的可维护性与安全性，实现资源的完整生命周期管理（上传至删除）、AI 对话的灵活编辑回溯，以及基于学生的精细化权限控制。

---

## 2. 功能详细设计

### 2.1 教师端资源管理（删除功能）
**需求描述**：教师拥有最高权限，可删除已发布的教材及关联的视频、文件。

**技术实现路径**：
1.  **数据清理链路 (Textbook 删除)**：
    *   **MySQL**: 物理删除 `textbooks` 表中的记录及其关联的 `resources` 记录。
    *   **MongoDB**: 删除 `textbook_contents` 集合中对应的解析 JSON。
    *   **Qdrant (RAG)**：调用 Qdrant API，通过元数据过滤器 `textbook_id == ID` 批量删除所有相关的向量点位（Points）。
    *   **磁盘文件**: 根据存储路径，从 `backend/uploads/` 目录下物理删除 PDF 文件。
2.  **数据清理链路 (Resource 删除)**：
    *   **MySQL**: 删除对应的视频/文件记录。
    *   **磁盘文件**: 物理删除对应的 `.mp4` 或其他附件。

**前端交互**：
*   在资源卡片（教材、视频、文件）增加“删除”按钮。
*   触发二次确认弹窗（防止误删导致向量数据丢失无法找回）。

---

### 2.2 AI 对话编辑与历史回溯
**需求描述**：用户可以修正之前的提问，AI 需基于修正后的上下文重新生成回答。

**业务逻辑**：
*   **截断模式 (Cascading Delete)**：当用户修改/删除第 $m$ 条对话时，该对话之后的所有历史记录（$m+1$ 到 $n$）将自动物理删除。
*   **重新生成**：修改第 $m$ 条提问后，系统自动触发一次新的 `/api/ai/ask` 请求，基于截断后的新上下文生成第 $m$ 条回答。

**技术实现路径**：
*   **后端**: 在 `chat_sessions` 集合中，利用 MongoDB 的 `$set` 操作符对 `messages` 数组进行切片重置。
*   **前端**: 气泡增加悬停操作栏。

---

### 2.3 教材访问权限管理 (ACL)
**需求描述**：教师可以控制哪些学生能看到哪些教材。

**数据模型变更 (MySQL)**：
*   `textbooks` 表增加字段：
    *   `visibility`: 0 - 公开（所有人可见）；1 - 私有（仅限白名单）。
    *   `allowed_student_ids`: 存储学生 ID 列表（格式：JSON 数组或逗号分隔字符串）。

**权限校验逻辑**：
1.  **列表检索**：学生请求 `/textbook/list` 时，SQL 增加查询条件：`WHERE visibility = 0 OR FIND_IN_SET(student_id, allowed_student_ids)`.
2.  **详情访问**：在 `GetContent` 接口层增加强制校验，防止学生通过拼接 URL 越权访问。

---

## 3. 核心 CheckList (技术细节)
*   **MySQL 确认**：本项目严格使用 MySQL 8.0，严禁引入 SQLite。
*   **Qdrant 隔离**：继续采用共享 Collection + Payload 过滤方案，删除时务必执行 `points/delete` 配合 `filter`。
*   **性能**：删除较大教材时，向量清理可能耗时，考虑在后端使用异步协程（Goroutine）执行 Qdrant 清理。

---
**文档版本**: v1.0
**状态**: 待评审
