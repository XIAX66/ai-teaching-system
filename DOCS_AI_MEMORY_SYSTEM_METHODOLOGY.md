# AI 智能助理记忆系统 (Memory System) 实现方法论

## 1. 概述
记忆系统旨在赋予 AI 助理“长期上下文”的能力。通过将用户与 AI 的每一轮对话持久化，并在下一次提问时将这些历史记录回馈给模型，使 AI 能够理解指代（如“它”、“刚才那个问题”）、维持对话连贯性并记住用户的偏好（如“我叫张三”）。

---

## 2. 核心架构：基于持久化的滑动窗口
系统采用 **MongoDB 存储 + 滑动窗口加载** 的模式。

### 2.1 数据流向
1.  **存储**：每轮对话（User Question & Assistant Answer）结束后，由后端 `AgentService` 自动追加到 MongoDB 的 `chat_sessions` 集合中。
2.  **提取**：新请求发起时，后端根据 `userID` 和 `textbook_id` 提取最近 N 条（当前设为 10 条）消息。
3.  **合并**：将“系统提示词 + RAG 背景知识 + 历史对话 + 当前问题”组装成一个完整的数组，发送给大模型。

---

## 3. 实现步骤
1.  **模型定义**：在 MongoDB 中建立 `ChatSession` 结构，包含消息角色（Role）、内容（Content）和时间戳。
2.  **Provider 升级**：将 `DoubaoProvider` 从单一 Prompt 接收升级为接收 `[]DoubaoMessage` 数组。
3.  **逻辑注入**：在 `AskStream` 接口中，强制先读取历史记录，再进行流式调用。
4.  **前端恢复**：在 React 组件挂载时（`useEffect`），首先请求历史记录接口，将历史气泡渲染在界面上。

---

## 4. 踩坑记录与解决方案 (关键)

### 4.1 豆包 Ark V3 协议的“洁癖” (400 错误)
*   **坑**：`v3/responses` 接口与 OpenAI 规范不完全兼容。它严禁直接传递字符串作为 `content`。
*   **解决方案**：每一条消息的 `content` **必须是一个数组**，即使是纯文本也必须包装成 `[{"type": "input_text", "text": "..."}]`。
*   **深坑**：字段互斥。在 `input_text` 对象中，绝对不能出现 `image_url` 字段（即使是空的或 `omitempty`）。我们通过 `map[string]interface{}` 动态构建对象解决了字段冲突。

### 4.2 键名不一致导致的“权限罗生门” (401/500 错误)
*   **坑**：JWT 中间件将用户信息存入 context 时键名为 `"userID"`，但我们在业务层错误地查找了 `"user_id"`。
*   **结果**：导致系统判定用户未登录（401）或在强转 `nil` 指针时 Panic（500）。
*   **解决方案**：全量检索并统一键名为 `userID`，并增加类型断言的安全校验（兼容 `uint` 和 `float64`）。

### 4.3 角色名不匹配 (Role Mapping)
*   **坑**：大模型只认识 `user`、`assistant` 和 `system`。我们的数据库之前记录了 `ai` 作为角色。
*   **结果**：模型无法识别 `ai` 角色，导致对话历史失效。
*   **解决方案**：在发送前进行映射转换：`if role == "ai" { role = "assistant" }`。

---

## 5. 最佳实践建议

1.  **限制上下文长度**：不要发送全部历史记录。随着对话增长，Token 消耗会指数级上升且容易超出模型限制。建议保留最近 5-10 轮对话。
2.  **Token 降级**：在开发阶段使用 `mini` 版本模型，验证逻辑通过后再切换回 `pro` 版本。
3.  **RAG 与记忆共存**：
    *   第一轮对话：执行向量检索获取背景。
    *   后续对话：优先依赖历史记忆，辅以向量检索。
4.  **异步持久化**：保存消息到数据库的操作不应阻塞 SSE 的流式输出，应在输出完成后或通过协程处理。

---
**总结**：记忆系统的本质是“上下文组装艺术”。在 Ark V3 时代，成功的关键在于**严格遵守其 JSON 结构规范**，并确保后端 Context 传递的准确性。
